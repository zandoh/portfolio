const styles = theme => ({
  main: {
    color: theme.palette.light
  },
  typistContainer: {
    display: "inline-flex",
    fontSize: "20px"
  },
  typist: {
    marginLeft: "4px",
    color: "blue"
  }
});

export default styles;
